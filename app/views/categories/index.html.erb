All categories

<% @categories.each do |category| %>
  <ul> <%= category.title %> </ul>
  <%= link_to "Edit", edit_category_path(category) %>
  <%= link_to "Delete", categories_path(@category), method: :delete %>
<% end %>